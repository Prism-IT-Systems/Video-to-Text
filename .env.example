# Transcription mode: "api" (OpenAI) or "local" (Python, fully offline)
TRANSCRIPTION_MODE=local

# OpenAI API Key - required only when TRANSCRIPTION_MODE=api
# OPENAI_API_KEY=sk-your-api-key-here

# Optional: Python path when TRANSCRIPTION_MODE=local
# PYTHON_PATH=python
